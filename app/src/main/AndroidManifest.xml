<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="thiago.com.br.myapplication" >

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".activitys.MainActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activitys.DashBoardActivity"/>
        <activity android:name=".activitys.ViagemActivity"/>
        <activity android:name=".activitys.GastoActivity"/>
        <activity android:name=".activitys.ViagemListActivity"/>
        <activity android:name=".activitys.GastoListActivity"/>
    </application>

</manifest>
